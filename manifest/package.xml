<?xml version="1.0" encoding="UTF-8"?>
<Package xmlns="http://soap.sforce.com/2006/04/metadata">
    <!-- Apex Classes -->
    <types>
        <members>*</members>
        <name>ApexClass</name>
    </types>

    <!-- Apex Triggers -->
    <types>
        <members>*</members>
        <name>ApexTrigger</name>
    </types>

    <!-- Custom Objects -->
    <types>
        <members>*</members>
        <name>CustomObject</name>
    </types>

    <!-- Custom Fields (just in case some fields not under object folder) -->
    <types>
        <members>*</members>
        <name>CustomField</name>
    </types>

    <!-- Layouts -->
    <types>
        <members>*</members>
        <name>Layout</name>
    </types>

    <!-- Flows (Visual Flows / Flow Builder) -->
    <types>
        <members>*</members>
        <name>Flow</name>
    </types>

    <!-- Validation Rules -->
    <types>
        <members>*</members>
        <name>ValidationRule</name>
    </types>

    <!-- Profiles -->
    <types>
        <members>*</members>
        <name>Profile</name>
    </types>

    <!-- Permission Sets -->
    <types>
        <members>*</members>
        <name>PermissionSet</name>
    </types>

    <!-- Sharing Rules -->
    <types>
        <members>*</members>
        <name>SharingRules</name>
    </types>

    <!-- Tabs -->
    <types>
        <members>*</members>
        <name>CustomTab</name>
    </types>

    <!-- Applications -->
    <types>
        <members>*</members>
        <name>CustomApplication</name>
    </types>

    <!-- Custom Labels -->
    <types>
        <members>*</members>
        <name>CustomLabel</name>
    </types>

    <!-- Workflow Rules -->
    <types>
        <members>*</members>
        <name>Workflow</name>
    </types>

    <!-- Lightning Pages (record/home/app pages) -->
    <types>
        <members>*</members>
        <name>FlexiPage</name>
    </types>

    <!-- Picklist Global Value Sets -->
    <types>
        <members>*</members>
        <name>GlobalValueSet</name>
    </types>

    <!-- Email Templates -->
    <types>
        <members>*</members>
        <name>EmailTemplate</name>
    </types>

    <!-- Static Resources -->
    <types>
        <members>*</members>
        <name>StaticResource</name>
    </types>

    <version>58.0</version>
</Package>
